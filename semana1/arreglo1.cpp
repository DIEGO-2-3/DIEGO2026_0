#include <iostream>

int main() {
    // Declaración (Imagen 2)
    int miLista[5] = {10, 20, 30, 40, 50};

    // Falta: Lógica para mostrar los datos
    for(int i = 0; i < 5; i++) {
        std::cout << "Indice " << i << ": " << miLista[i] << std::endl;
    }

    return 0;
}